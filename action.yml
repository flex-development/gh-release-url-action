# ACTION METADATA
# https://docs.github.com/actions/creating-actions/metadata-syntax-for-github-actions

---
name: gh-release-url
author: '@flex-development'
description: Create a URL for a GitHub release
inputs:
  owner:
    default: ${{ github.repository_owner }}
    description: github repository owner
    required: false
  repo:
    default: ${{ github.event.repository.name }}
    description: github repository name
    required: false
  server:
    default: ${{ github.server_url }}
    description: github server url
    required: false
  tag:
    description: the release tag or version to create a url for
    required: true
  tag-prefix:
    description: the prefix to append to the release version
    required: false
outputs:
  url:
    description: the release url
    value: ${{ steps.url.outputs.result }}
runs:
  using: composite
  steps:
    - id: url
      shell: bash
      run: |
        set -e

        # capture full repository name.
        repository=$(echo "${{ inputs.owner }}/${{ inputs.repo }}" | xargs)

        # capture release tag.
        tag=$(echo "${{ inputs.tag-prefix }}${{ inputs.tag }}" | xargs)

        # capture release url.
        url=${{ inputs.server }}/$repository/releases/tag/$tag

        # set output.
        echo "result=$(echo "$url" | xargs)" >>$GITHUB_OUTPUT
branding:
  color: blue
  icon: link
